package com.example.demo.src.image.model.entity;

import org.springframework.data.jpa.repository.JpaRepository;

public interface MotelRoomImageRepository extends JpaRepository<MotelRoomImage,Long> {
}
